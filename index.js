// Get started!
const name = prompt("What's your name?");

const age = 16;

console.log(`You are, ${age},years old` );

console.log(`Welcome to my game, ${name}` );

let choice = prompt(`What sport do you want to play?, (football, basketball, soccer)`);

console.log(`Okay, ${name}, your sport is now ${choice}`);

let role = prompt("What is your role?")

role = parseInt(role)